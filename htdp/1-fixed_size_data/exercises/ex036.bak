;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ex036) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;Exercise 36. Design the function image-area, which counts the number of pixels in a given image.

(require 2htdp/image)

;; Image -> Natural
;; Given an image, return its area (* width height).
(check-expect (image-area (square 11 "solid" "Blue")))
(check-expect (image-area (rectangle 10 40 "solid" "Deep Green")))

(define (image-area img) 1)